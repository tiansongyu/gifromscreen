name: Linux portable package

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: portable-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ubuntu-22-portable:
    runs-on: ubuntu-22.04
    timeout-minutes: 45
    steps:
      - name: Check out source
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6

      - name: Install build and smoke-test dependencies
        run: |
          sudo apt-get update
          sudo apt-get install --yes \
            clang libclang-dev libpipewire-0.3-dev libwayland-dev \
            libxkbcommon-dev libxkbcommon-x11-dev libxcb-shape0-dev \
            libxcb-xfixes0-dev libegl1-mesa libgl1-mesa-dri \
            libvulkan1 mesa-vulkan-drivers desktop-file-utils xvfb xdotool

      - name: Install pinned Rust toolchain
        uses: dtolnay/rust-toolchain@6bed0761d98439e5a578e2877258200ad565ba87
        with:
          toolchain: 1.88.0

      - name: Cache portable build dependencies
        uses: Swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6 # v2
        with:
          workspaces: ". -> target/portable-build"
          key: ubuntu22-portable-rust188
          save-if: ${{ github.ref == 'refs/heads/main' }}

      - name: Build real portable archive
        run: python3 scripts/build_portable.py --output-dir target/package-first

      - name: Test build identity and target isolation
        run: python3 scripts/test_build_portable.py

      - name: Test AppImage development packaging boundaries
        run: |
          python3 scripts/test_appimage_tools.py
          python3 scripts/test_build_appimage.py
          python3 scripts/test_build_appimage_runtime.py
          python3 scripts/test_collect_runtime_sources.py
          python3 scripts/test_collect_ubuntu_sources.py
          python3 scripts/test_appimage_native.py
          python3 scripts/test_owned_process.py

      - name: Verify deterministic repackaging
        run: |
          python3 scripts/build_portable.py --skip-build --output-dir target/package-second
          cmp target/package-first/*.tar.gz target/package-second/*.tar.gz

      - name: Inspect archives and test safe installation
        run: python3 scripts/test_portable.py target/package-first/*.tar.gz

      - name: Test owned display lifecycle
        run: |
          python3 scripts/test_owned_xvfb.py
          python3 scripts/test_smoke_portable_desktop.py

      - name: Launch packaged native desktop
        run: python3 scripts/smoke_portable_desktop.py --owned-xvfb target/package-first/*.tar.gz

      - name: Upload tarball and SHA256 checksum
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: gifromscreen-linux-x86_64-ubuntu22-${{ github.sha }}
          path: |
            target/package-first/*.tar.gz
            target/package-first/*.tar.gz.sha256
          compression-level: 0
          if-no-files-found: error
          retention-days: 30
